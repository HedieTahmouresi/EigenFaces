"""Streamlit demo. Thin wrapper only -- all logic lives in src/.

Enrollment + rejection-threshold demo (see project plan, section 2:
"1-NN always returns *something*; without these the demo is meaningless.").
"""

import streamlit as st

st.title("Eigenfaces")
st.write("Demo not yet implemented.")
